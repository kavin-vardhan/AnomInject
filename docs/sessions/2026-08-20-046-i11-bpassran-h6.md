# 2026-08-20 — 046 — `I11`: the `bPassRan` hypothesis (`H6`), pre-declared and measured

**Plugin:** `AnomalyInjector` — **docs only. ZERO production code. NO tag. `m26` untouched.**
**Bench:** `CaptureBench` — not used this session; no harness change.
**Base:** `d6bee7a` (tag `m26`) → `2bc51ec` → `677301a` → `6215de0` → `4f5e774` → `8a42809` → `4dcde6d`.

> 🧭 **COLD READER: `m26` IS STILL SHIPPED AND TAGGED. Nothing was reverted and no fix exists.**
> What changed is that **`m26`'s SAFETY ARGUMENT is corrected by measurement.** The correction is
> stated ONCE, in `docs/invisible-anomaly-mechanisms.md` → *"SAFETY-PROPERTY CORRECTION — the admit
> bias is sound at the enum and unsound at the assignment"*. This journal is how it was reached.

---

## 1. What this session did, in one read

`m26` deletes an event when its target is `MEASURED_ZERO`. The safety argument for that rested on
the admit bias: a target that was never measured lands `NOT_MEASURED` and is always admitted.

**A source read found that the precondition separating those two states is a VIEW-LEVEL quantity
used as a PER-TARGET one.** That was written down as a hypothesis, numbered, pre-declared with its
branches and verdicts, and then measured. **It reproduced, on two independent routes.**

| | |
|---|---|
| hypothesis | **`H6`** — "the `bPassRan` hypothesis" |
| status | ✅ **SUPPORTED, two independent routes, five legs, every gate passed** |
| scope | ⛔ **PIE only (`G76`) — MECHANISM CLAIM ONLY. NO INCIDENCE CLAIM. The lever was CONSTRUCTED.** |
| open | whether the SHIPPING path supplies its own writer — **`I11-B`** |
| fix | ⛔ **NONE. Not designed, not proposed, not queued. Owner ruling: `I11-B` first.** |

---

## 2. `H6`, and why the defect made its own test constructible

`bPassRan` (`AnomalyMaskMeasure.cpp:370`) tests `Mask.CustomStencilExtent` — **1×1 (the engine's
`StencilDummy`) or view-sized.** That answers *"was custom depth produced AT ALL for this view this
frame"*. It is used as *"did THIS TARGET write custom depth"*.

Tags accumulate: `AnomalyStencilTag.cpp` does not untag until `RestoreAll()` at `EndRun`. And the
extent goes view-sized if **any** primitive in the scene writes custom depth — it does not have to
be ours, and it does not have to carry a reserved tag.

⇒ **a target that contributed no evidence whatever can pass `bPassRan` and contribute a clean
`Count = 0`, which is `MEASURED_ZERO`, which is VETOED.**

### 2.1 🆕 THE IRONY, RECORDED BECAUSE IT READS AS LUCK OTHERWISE

The test was constructible because of **`H5`'s own defect**. Every gate on the arm path tests a
**FLAG**, never on-screen-ness:

| site | test | what it actually reads |
|---|---|---|
| `AnomalyMaskMeasure.cpp:174` (`LOCK-1`) | `AActor::IsHidden()` | the `bHidden` FLAG — what `blinking` toggles |
| `AnomalyStencilTag.cpp:41` (`TagActor`) | `IsRenderableComponent` = `IsVisible()` ∧ type | a component visibility FLAG, plus a TYPE |
| `AnomalyMaskMeasure.cpp:454` (`SampleEndOfFrame`) | `AActor::IsHidden()` again | the same FLAG |

**None of them asks whether the target is on screen.** That is exactly `H5` — *renderable TYPE,
FLAGGED VISIBLE, weaker than DRAWS PIXELS* — and it is why the mask will earnestly go and measure
something standing behind the camera. **The defect under investigation is what makes the
investigation possible.**

---

## 3. `I11-A` — the instrument

**Pre-declared at `6215de0`, amended twice (`4f5e774`, `8a42809`), BOTH AMENDMENTS BEFORE ANY
MEASUREMENT EXISTED, both with ZERO DELETIONS.** The original three arms are preserved under a
`SUPERSEDED` banner rather than deleted, so the record shows the restructuring could not have been
biased by a result.

**THE STANDING RULE THAT GOVERNS AMENDMENTS, written into the file as a rule:**

> A pre-declaration may be amended before any measurement exists, and ONLY to TIGHTEN a validity
> condition. It is never amended to LOOSEN one, and a prediction or a verdict is never amended at
> all once the instrument exists. Every amendment is dated, appends, and deletes nothing.

### 3.1 Why the arms were restructured

The first design needed **two NAMED targets in one run**. Targeted mode carries exactly one
`(anomaly, actor)` pair for the whole run (`AnomalyCaptureSubsystem.cpp:690-693`, `:1237`), and the
auto-pool cannot be told whom to pick. **That is journal 045 PART ONE blocker `B1`**, which halted
`H4`'s original brief for the same reason.

**The lever replaced the second target with a switch:** one non-target actor with
`bRenderCustomDepth = TRUE` and `CustomDepthStencilValue = 1` — outside the reserved 200/255 range.
**One variable instead of two, and the lever's own firing is checkable independently of every arm.**

⚖ **The owner reopened the host-custom-depth scoping deliberately to allow this**, recording that
the earlier exclusion had been *reasoning about RISK SURFACE when the question was INSTRUMENT
DESIGN*. The scoping stands for the risk; it does not stand against using the route as a lever.

### 3.2 Stencil value 1 confirmed inert at FIVE stages, not one

`ReservedStencilBase = 200` (`AnomalyStencilTag.h:9-10`) · shader returns 0 below the base
(`AnomalyVisibleMask.usf:26-30`) · the CPU scan skips `V == 0` (`…SceneViewExtension.cpp:213-217`) ·
the tag loop walks only `[200,255]` (`:228`) · `bSawUnassignedReservedTag` needs a reserved tag with
`Count > 0` (`:242`) · `VerifyActorStillTagged` walks only record targets.

### 3.3 The five legs

**The same actor is present in every leg. The only thing that moves is one boolean on it.**

| # | leg | lever | target |
|---|---|---|---|
| 1 | `I11A_L1_CRAMP` | OFF | `SM_Ramp2…2086822137` — **Nanite**, on screen |
| 2 | `I11A_L2_CGRAT` | OFF | `StaticMeshActor…1260804314` (`SM_GratIng`) — **non-Nanite**, off screen |
| 3 | `I11A_L3_ARMA` | **ON** | as leg 1 |
| 4 | `I11A_L4_ARMB` | **ON** | as leg 2 |
| 5 | `I11A_L5_CRAMP2` | OFF | as leg 1 — **return to baseline** |

Lever actor: `BP_Lamp_C…2086829166 / LampMesh` — non-Nanite, 4.0° off-axis, bounds radius 55 cm.

**Nanite status by DIRECT PROPERTY READ** (`StaticMesh.nanite_settings.enabled`) rather than by
`nanite_signature_scan.py`. That is a MEASUREMENT and it **corroborates** the signature scan rather
than replacing it silently: `SM_Ramp` reads Nanite `TRUE`, as PARTS TWENTY-SIX/TWENTY-SEVEN
concluded. Free census: **of 350 static-mesh actors in MainWorld, 244 Nanite / 72 non-Nanite** —
`G134`'s *"overwhelmingly Nanite on this title"* is now direct, not inferred.

### 3.4 The precondition that made Arm C a control rather than a hope

**982 primitive components scanned across MainWorld's 425 actors: ZERO pre-existing custom-depth
writers.** So with the lever OFF, nothing else can produce custom depth and the 1×1 expectation is
grounded in a measurement of the level.

---

## 4. Results — every gate, in the order the file requires

### 4.1 LEVER VALIDITY — PASS

| leg | lever | `M23 PASS` lines | `customStencilExtent` |
|---|---|---|---|
| 1 | OFF | 58 | **1×1** ×58 |
| 2 | OFF | 58 | **1×1** ×58 |
| 3 | **ON** | 29 | **876×872** ×29 |
| 4 | **ON** | 29 | **876×872** ×29 |
| 5 | OFF | 58 | **1×1** ×58 |

**The lever fired, on a quantity independent of every arm's outcome.** ⚠ **876×872, not the
1280×720 the pre-declaration wrote** — see §7 / `G138`.

### 4.2 ARM C, THE CONTROL — PASS, AND THE WORLD CAME BACK

Legs 1, 2 and 5: **8 events each, `NOT_MEASURED` ×8, `notMeasured=8`, 0 vetoed.**
**Leg 5 matches leg 1 on BOTH quantities the rule named** — `NOT_MEASURED` and extent back to 1×1.
The return-to-baseline invalidator did not fire.

### 4.3 VIEW-RELATIONSHIP GATES — PASS

🚨 **Legs 3 and 4 vetoed ALL EIGHT events, so their `annotation.json` holds ZERO anomalies and
`coverage_ratio` is GONE from it.** The evidence comes from `labels.jsonl`, which survives the veto.

| leg | target | `annotation.json` anomalies | `labels.jsonl` `bbox_valid` (59 rows) |
|---|---|---|---|
| 1 | ramp OFF | 8 | TRUE 59 / FALSE 0 — **on screen** |
| 3 | ramp **ON** | **0** | TRUE 59 / FALSE 0 — **on screen** |
| 5 | ramp OFF | 8 | TRUE 59 / FALSE 0 — **on screen** |
| 2 | grat OFF | 8 | TRUE 0 / FALSE 59 — **never projected** |
| 4 | grat **ON** | **0** | TRUE 0 / FALSE 59 — **never projected** |

`coverage_ratio` read from `annotation.json` for legs 1 and 2: **0.037911836034739861** (identical
to 16 s.f. on all 8 events) and **0**. **`bbox_valid` DISAMBIGUATES leg 2's zero** as the
never-projected sentinel rather than zero clamped area — the ambiguity recorded in the amendment
(`AnomalyCaptureSubsystem.cpp:1985` emits `0.0` for both) resolved by measurement.

### 4.4 `ArmsIssued > 0` — PASS
`arms=4` on every event on every leg (`arms=1` on each leg's frame-capped final event). **The
false-confirmation route — `NOT_MEASURED` produced by never reaching the precondition — did not
occur, and it was excluded by construction rather than spotted afterwards.**

### 4.5 THE DISCRIMINATOR

| lever OFF | | lever ON | |
|---|---|---|---|
| leg 1 ramp | `NOT_MEASURED` ×8 → **ADMITTED** | leg 3 ramp | **`MEASURED_ZERO` ×8 → VETOED** |
| leg 2 grat | `NOT_MEASURED` ×8 → **ADMITTED** | leg 4 grat | **`MEASURED_ZERO` ×8 → VETOED** |
| leg 5 ramp | `NOT_MEASURED` ×8 → **ADMITTED** | | |

`G-11` on legs 3 and 4 alike: `countedEventsBefore=8 countedEventsAfter=0 vetoedEvents=8
nonManifestedEvents=0`.

**THE MECHANISM IS VISIBLE IN THE COUNTERS, not only in the outcome:**

```
lever OFF   framesNoPass=4  framesContributed=0             -> NOT_MEASURED
lever ON    framesNoPass=0  framesContributed=4  maxCount=0 -> MEASURED_ZERO
```

**Four frames carrying NO EVIDENCE ABOUT THE TARGET were CONTRIBUTED as clean zeros**, because a
lamp elsewhere in the scene took the view-level extent non-dummy.

**VERDICT, pre-registered and quoted from `8a42809`:** *"Arms 1 AND 2 both MEASURED_ZERO, arm 3
clean → SUPPORTED by two independent routes. m26 has a delivery-affecting defect. STOP."*

### 4.6 The collisions observation — REPORTED, NOT EXPLAINED

`CollisionHits` differs **systematically** with the lever: **4 on every lever-OFF event**
(*"unassigned reserved tag 255 observed"*), **0 on every lever-ON event** — the `StencilDummy` 255
detector firing exactly when the dummy is bound. ⛔ **It does not enter the verdict and it is not
explained here.**

---

## 5. 🆕 OBSERVATION — MainWorld's settled pose VARIES PER PIE SESSION

**n = 5 settled legs.** Read from `PlayerCameraManager` immediately before each capture, and
corroborated by each leg's own artifacts.

| leg | camera location | rotation (pitch, yaw, roll) |
|---|---|---|
| 1 | `3072.78, 4334.52, 1636.01` | `-0.175, **-39.824**, 0.000` |
| 2 | `3115.78, 4379.98, 1634.48` | ` 0.000, **-46.999**, 0.000` |
| 3 | `3073.76, 4335.69, 1634.48` | `-0.000, **-39.999**, 0.000` |
| 4 | `3073.76, 4335.69, 1634.48` | `-0.000, **-39.999**, 0.000` |
| 5 | `3073.76, 4335.69, 1634.48` | `-0.000, **-39.999**, 0.000` |
| *(packaged reference, journal 045 PART SEVEN)* | `3073.76, 4335.69, 1634.48` | `0, -39.999, 0` |

**Legs 3, 4 and 5 reproduce the packaged settle EXACTLY on all six components.** Leg 1 is 0.175°
off. **Leg 2 is 7.175° off and 62.59 cm away.**

**WITHIN a session the camera is perfectly still** — `labels.jsonl` shows **1 distinct view origin
across all 90 rows**, on every leg.

⛔ **CAUSE NOT ESTABLISHED. CANDIDATES ENUMERATED, NONE ADOPTED, NOT RANKED (`G120`):**
1. **`A47`'s rotation bifurcation** — ⚠ **ADJACENT, NOT IDENTICAL.** `A47` was characterised on
   `CB_GateLevel` with a fixed `PlayerStart` and all-static actors; this is MainWorld with a
   settling `BP_Bot_C` pawn.
2. **unattended motion** nudging the pawn (MainWorld has `BP_Stomper_C` and friends).
3. **the 30 s focus-gate wait** — every leg settles for 30 s before capture, and a longer settle
   gives any per-session difference more time to accumulate.

🚨 **ANY FUTURE CROSS-SESSION COMPARISON IN MAINWORLD INHERITS THIS.** *(A discarded sixth Play,
never captured, read `-38.599` at pitch `-12.25` — mid-settle, so not a settled pose and not in the
series.)*

### 5.1 THE POSE RULING — every gate here is CATEGORICAL

⛔ **A TOLERANCE WAS PROPOSED BY CODE AND REFUSED BY THE OWNER, INCLUDING CODE'S OWN
RECOMMENDATION.** *"A tolerance is a validity constant with NOTHING CALIBRATING IT… That is
`G135`'s exact shape and it is the same move the project refused when it declined the ratio rule for
the veto."* **Do not propose one later.**

**On screen or off. Wrote custom depth or did not. `MEASURED_ZERO` or `NOT_MEASURED`. NOT ONE OF
THEM IS A DISTANCE.** A pose delta matters only if it FLIPS A CATEGORY, and whether it does is
MEASURED PER LEG, never predicted. **It did not flip one:** leg 2 at −46.999 and leg 4 at −39.999
both read `bbox_valid FALSE` on 59/59 rows.

⚠ **And `framesNoPass` is BARRED as view evidence on BOTH arms** — it is downstream of the lever,
and `H6` predicts precisely that it stops firing when the lever is on. Using it would make the arm
unfalsifiable. **`coverage_ratio`/`bbox_valid` come from `ProjectActorBoundsToScreenRect`, a
separate CPU path with no relationship to custom depth.**

---

## 6. 🆕 `L3` IS LOAD-BEARING — AND IT IS NO LONGER A PREDICTION

`L3` (the `annotation.json` / `labels.jsonl` disagreement) sits on the forward plan as a defect to
fix. **`I11-A` now depends on it.**

Legs 3 and 4 vetoed every event, so their `annotation.json` carries `anomalies: []`. **Their entire
view-relationship evidence exists ONLY in `labels.jsonl`** — the file that is *not* corrected by the
veto. Had `L3` already been "fixed", the gate that validated arms A and B would have had no source.

⛔ **NOBODY FIXES `L3` UNTIL THIS LINE OF WORK CLOSES.** Recorded here so a future session does not
tidy it away as loose ends.

---

## 7. 🆕 GOTCHAS MINTED THIS SESSION — `G136`, `G137`, `G138`

Full text in `docs/gotchas.md`. In brief:

- **`G136` — an ABSENCE-OF-FINDING is only as good as the SURFACE that was searched.** Code
  foreclosed scripted Play-In-Editor by enumerating the `unreal` namespace; the owner challenged it
  and named the right surface — the **bridge's own command list**, which is custom and C++-side. The
  foreclosure SURVIVED (62 endpoints enumerated by category, none starting Play or Simulate) **but
  it survived a test it had not been given.** `G120` says a false foreclosure is never re-run by
  definition; here the owner's memory was the only thing that tested it.
- **`G137` — a VIEW-LEVEL property used as a PER-TARGET precondition admits evidence that does not
  exist.** The `H6` mechanism as a transferable lesson: *"was the instrument running at all"* and
  *"did the instrument see THIS subject"* are different questions, and a per-frame/per-view answer
  substituted for a per-subject one turns "no evidence" into "measured zero".
- **`G138` — an expectation written for one environment and read in another.** The pre-declaration
  said the lever would take the extent to `1280×720`; a PIE panel is `876×872`. **THE GATE SURVIVED
  BECAUSE IT WAS WRITTEN CATEGORICALLY — "must DIFFER" — RATHER THAN AS A VALUE.** Write validity
  gates as categories wherever the category is what you mean.

---

## 8. 🆕 A REUSABLE PATTERN — the single-boolean lever with a return-to-baseline leg

Worth reaching for again:

1. **Test a validity instrument with ONE BOOLEAN on an actor unrelated to any target.** One
   variable, and the treatment cannot be confused with the subject.
2. **Make the lever's own firing independently observable** — here `customStencilExtent`, which
   already existed in the log, so the pre-authorised extra log line was **never taken**.
3. **Run the control BEFORE and AFTER**, with a return-to-baseline leg whose job is to prove the
   world came back. If leg 5 had not matched leg 1, the whole run was void.
4. **Gate the treatment leg on the treatment being present** — each ON leg verified the writer in
   the **PIE world**, not merely in the editor world, and would not have started without it. That
   check is what caught an early un-levered Play and got it thrown away instead of counted.

---

## 9. Open questions recorded, none investigated

- 🆕 **CHAT-SIDE SPECULATION, UNVERIFIED, NOT A MECHANISM, NOT TO BE ACTED ON.** The collisions
  observation (§4.6) sits next to PART TWENTY-ONE's recorded refutation that moving
  `ReservedStencilMax` would have *silenced the 255 detector and made every event return a
  clean-looking `MEASURED_ZERO`*. That raises a **question** about what role the 255 detector plays
  when the dummy is bound, and whether it is load-bearing in a way nobody designed. ⛔ **Attributed
  to chat. Belongs to fix design. Fix design is NOT authorised.**
- **`SM_GratIng` is a COMPLEX-SILHOUETTE, NON-NANITE mesh with ~10 instances in MainWorld.** Journal
  §209 refused a ratio rule because *"a legitimate target with a COMPLEX SILHOUETTE (fence, railing,
  ladder, grate, sparse foliage)… NO SUCH TARGET EXISTS IN OUR MEASURED SET."* **One exists, in the
  shipped level, and it is measurable.** ⛔ **RECORDED, ACTED ON NOWHERE. Its drawn-to-claimed ratio
  was DELIBERATELY NOT MEASURED in any leg, by instruction.** The veto rule is ZERO-ONLY and LOCKED.

---

## 10. State after this session

| | |
|---|---|
| plugin production code | **ZERO lines changed** |
| tag | **none new** · `m26` still at `d6bee7a`, NOT rewritten |
| `feature/stencil-capture` | **READ-ONLY at `76cac74`**, never checked out |
| `P6` | **DOES NOT MOVE** |
| level on disk | **UNTOUCHED** — `MainWorld.umap` 21,139 B / mtime `2026-06-09 21:10:26`; `__ExternalActors__` 419 files / 8,182,199 B / newest `2026-06-18 17:36:32`. **Byte- and mtime-identical before and after. NOTHING WAS EVER SAVED.** |
| lever | **CLEARED**, writer census back to **ZERO** |
| banked | `I11A_L1_CRAMP` `…-172633` · `I11A_L2_CGRAT` `…-172924` · `I11A_L3_ARMA` `…-173956` · `I11A_L4_ARMB` `…-174208` · `I11A_L5_CRAMP2` `…-174510` — each with the session, a per-leg log slice and `_leg_meta.json`; 90 frames / 0 zero-byte / 5 artifacts on every leg |
| ⛔ **NOT done** | **no fix designed, proposed or queued.** Owner ruling: **`I11-B` first** — it is the BEFORE picture, and it may be answerable from banked evidence for free |

**WHAT THIS SESSION SETTLES:** `m26`'s structural claim about the enum stands, and the safety
argument built on it does not. A target the instrument cannot see can be deleted as though it had
been seen and found empty — measured, on two routes, with the world proven to have come back. **What
it does NOT settle is whether that reaches a real run without a lever somebody built on purpose.**

---

## 11. `I11-B` STAGE 1 — IT DOES REACH A REAL RUN. BRANCH `Y-1`.

Pre-declared at `f8a96db` **before any reading**, with a **DECLARATION OF PRIOR EXPOSURE** in the
file: `S4` fully seen (so it carries no predictive weight), `S2`/`S3` partly seen, **`S1` wholly
fresh — and `S1` is the reading the branches turn on.**

🚨 **A RESCUE FIRST: the three play-gate smoke sessions were UNBANKED**, sitting in
`Saved\AnomalyCaptures`. Banked as `I11B_SMOKE_RESCUE`, file-count and byte-total verified at the
bank, together with the smoke's own log — which survived the editor restart as
`StackOBot-backup-2026.08.20-10.09.49.log`, **394,508 bytes, exactly the size PART THIRTY-TWO
cites**. *Fourth time unbanked evidence has surfaced in this project; this time it was the evidence
the question needed.*

| reading | result, auto-pool smoke `session_20260820-153241` |
|---|---|
| **S1** | `customStencilExtent` **876×872 ×26**, `1×1` ×6. **MainWorld has ZERO pre-existing custom-depth writers, so that extent came from OUR OWN TAGS.** *(Targeted tests A and B: 876×872 ×29 each.)* |
| **S2** | 8 events, `notMeasured=0`. Four `MEASURED_NONZERO` (4,519 / 54,779 / 3,800 / 6,661 px), four `MEASURED_ZERO` — three with `noPass=1 contrib=3`, one with `noPass=0 contrib=4` |
| **S3** | 🎯 **BOTH `MEASURED_ZERO` TARGETS ARE WHOLLY NANITE, by direct property read** — `BP_Stomper_C` (`SM_Fan_Frame`, `SM_GenericMachine_Engine`) and `RoomBuilderSquare_C` (4 ISM comps, all Nanite). **Every event reads `bbox_valid` TRUE on all rows — nothing was off screen.** The other tagged targets are non-Nanite and drawing |
| **S4** | `countedEventsBefore=8 countedEventsAfter=4 vetoedEvents=4 nonManifestedEvents=0` |

⇒ **`H6` FIRES UNAIDED IN THE SHIPPING PATH.** The plugin's own accumulated tags on non-Nanite
targets take the extent view-sized, and Nanite targets that cannot write custom depth at all
contribute clean zeros and are vetoed.

⛔ **THE SMOKE'S FOUR VETOES ARE NOT ATTRIBUTED TO `H6`.** It was **present and active** in that
run. That is not the same as having **caused** them, and the pre-registration said so before the
reading (`G120`).

## 12. ⚖ OWNER DECISION — `H6` IS DOCUMENTED, NOT FIXED

⚠ **SUPERSEDED IN PART BY §13 BELOW: THE OWNER LATER REVERSED THIS AND AUTHORISED `m27`.**
**What did NOT change is the `H6` half - it remains DOCUMENTED, NOT FIXED, and `m27` is not an
`H6` fix.** The sentence below is left as written because the sequence is part of the record.

**No `m27`. No fix. No veto-default change. Nothing queued against the code.** The reasoning, and
the two-cvar condition it rests on, are in the ledger entry **"`H6` — DOCUMENTED, NOT FIXED"** and
are not restated here.

**VERIFIED FROM 5.1 SOURCE THIS SESSION (Task M), and the answer is TRUE *with a condition*:** with
`Support Nanite` unticked a Nanite-flagged mesh falls through to a conventional
`FStaticMeshSceneProxy`, **which does set `bRenderCustomDepth`** (`StaticMeshRender.cpp:1936`) —
so it becomes measurable. ⚠ **But the fallback is gated on a SECOND cvar,
`r.Nanite.ProxyRenderMode` (`StaticMeshRender.cpp:127-140`, default `0`); non-zero returns
`nullptr` and the mesh renders NOTHING.** The condition is **two settings, not one**, and the
second is a **scalability** cvar an ini or device profile can move. **That was reported rather than
smoothed into the answer the decision wanted.**

**`I11-B` is CLOSED AT STAGE 1. Stage 2 is UNRUN, NOT CANCELLED** — its pre-declaration stands as
written. Two new boxes in `PRE-DELIVERY-CHECKLIST.md` §1 carry the condition forward, including the
one nobody has ever assessed: **whether the host game itself writes custom depth**, which would
supply `H6`'s precondition permanently and independently of Nanite.


---

## 13. ⚖ OWNER REVERSAL — `m27` IS AUTHORISED AFTER ALL

⚠ **§12 ABOVE SAYS "No `m27`". THAT WAS TRUE WHEN IT WAS WRITTEN AND IS SUPERSEDED BY THIS SECTION.**
It is annotated rather than rewritten: the sequence matters, and a journal that quietly edits its own
earlier state stops being a record.

**What did NOT change: `H6` remains DOCUMENTED, NOT FIXED.** No fix for any route was designed,
proposed or queued, including route (e). `m27` is not an `H6` fix and must never be read as one.

**What `m27` actually is — three unrelated problems that all became visible while `H6` was being
written up:**

1. **The `m26` cure did nothing in a delivered build.** `bMaskMeasure`'s compiled default is `false`
   and there was no ini key, so a client build labelled exactly as `m25` did — the
   invisible-anomaly behaviour that started this whole line of work.
2. **The log was SILENT about the mask when it was off.** The only banner lived inside
   `if (bMaskMeasure …)`, so a delivered session with a **missing, misspelt or `G88`-silenced key**
   produced a log **byte-identical** to one where the mask was deliberately off. The failure and the
   intended state were indistinguishable in the only artifact a client returns. → **`G139`**.
3. **Foliage labels are unusable**, and the delivery bundle had drifted away from a workflow the
   client could actually run.

🚨 **THE LOAD-BEARING PART IS NOT THE KEY — IT IS ITEM 2.** A key that reports nothing has the same
failure mode as no key at all.

### 13.1 ⚖ RULING 1 WAS ISSUED, BUILT, AND THEN REVERSED MID-BUILD

Chat ruled a **two-key split** (`bMaskMeasureDefault` gating measure+report, `bMaskVetoDefault`
gating the veto alone, plus an incoherent-combination guard and six Gate-3 legs). It was **fully
built**. The **owner then reversed it**: one key, deletion on, nothing extra to explain in delivery.

📌 **HOW THE REVERSAL WAS EXECUTED, because the method is the point:** both subsystem files were
reverted **to HEAD and rebuilt** rather than unpicked surgically. **A clean redo cannot leave
residue; a surgical removal can.** A repo-wide residue sweep followed, and it reported the one
pre-existing unrelated hit rather than claiming zero.

---

## 14. `m27` AS-BUILT

**Plugin, three commits** (`0d5e458` capture · `409b67a` viewport · `9f86600` client docs) and
**AnomDash, two** (`2a62c2b` delivery · `b8273c2` host-tools README).

### 14.1 DELIVERABLE A — `bMaskMeasureDefault`

`[AnomalyCapture] bMaskMeasureDefault`, following `bSveCaptureDefault`'s exact shape **including its
provenance flag**. **The compiled default STAYS `false`**; the ini carries the delivered behaviour;
`IAI.Capture.Mask` still overrides at runtime. One key gates measure + report + veto together,
faithful to the code as it stands.

**The client block is two lines:**

```ini
[AnomalyCapture]
bMaskMeasureDefault=True
bDeliveryModeDefault=True
```

### 14.2 DELIVERABLE A2 — THE VETO READOUT, so an accepted cost stops being an unmeasured one

One log line per removed event, prefixed **`VETOED-OBJECT`** — **a token that appears exactly once in
the codebase**, so grepping a log lists precisely the objects that went. It carries target,
`asset_name`, `component_class`, mask state, `maxCount` and a **translucency verdict**.
`run_summary` gains **`translucent_vetoes`** and **`translucency_unknown_vetoes`**.

Translucency is read via `GetMaterial(slot)->GetBlendMode()` — the virtual that `UMaterialInstance`
overrides — so an instance overriding an opaque base is seen correctly; that is the weakness the
engine itself warns about at `MaterialShared.cpp:1758`. Components are selected with
**`IsRenderableComponent`, the same predicate `TagActor` uses**, so it inspects exactly what was
tagged. **UNKNOWN gets its own token AND its own counter** and is never folded into opaque — an
unknown that reads as a negative makes a future count wrong in the comfortable direction.

⛔ **DIAGNOSTIC ONLY. It feeds nothing, gates nothing, and must never become a filter.** The veto
rule is unchanged and still switches on the enum alone; `MaxCount` is still never read by it.
**`annotation.json`'s field set is untouched. `P6` does not move.**

📌 **A grep-pollution trap caught in my own fresh work:** the summary line originally contained the
literal `VETOED-OBJECT` while telling the reader to grep for it — which would have added one junk hit
to **every future search, forever**, and would have looked helpful. The summary points at
`client-delivery.md` instead.

### 14.3 DELIVERABLE A3 — the bisect switch says what it is

`IAI.Capture.Mask`'s help is rewritten to state that it is **THE BISECT** and that it takes effect
**BETWEEN RUNS, not mid-run**. No behaviour change — the mid-run guard was always there. ⚠ **The old
help also still described `m26` as if the veto were unconditional**, which is now stated accurately.

### 14.4 FOLIAGE EXCLUDED FROM SELECTION

`AInstancedFoliageActor` is excluded in `IsRenderableComponent`, placed **after** the `IsVisible()`
test and **before** the ISM instance-count test. Both live entry points inherit it from the one
predicate, so selector, auto-injector and the dashboard read-back move together. Hard removal per
`G33` — no toggle, no cvar, no ini key.

**`Foliage` was added to `AnomalyInjector`'s PRIVATE deps by owner ruling**, recorded in `CLAUDE.md`
and `architecture.md` as a **dated ruling rather than left as drift** — the treatment `InputCore` got
at `m5`.

🚨 **A CLASS-NAME STRING MATCH WAS REFUSED, and the reason is the rule:** a rename would make a name
match **silently stop excluding**, whereas a type reference **breaks the build**. A compile error is
the loudest failure available; a missing check must never read as a passed check. ⛔ **No string
match was added alongside it** — two mechanisms means one can rot unnoticed while the other covers
for it.

---

## 15. 🆕 `MinimalAPI` — why `IsA<T>()` links anyway

`AInstancedFoliageActor` is **`MinimalAPI`**, so its **member functions are not exported**. That
sounds like it should defeat the exclusion, and it does not: **`MinimalAPI` DOES export
`StaticClass()`, which is all `IsA<T>()` needs.** The clean link is the proof, not the reasoning —
but the reasoning is recorded so a future reader does not "fix" a non-problem by reaching for a
string match.

---

## 16. 🆕 THE `config.json` RELOCATION — a doc that was stale BY ERA, not by neglect

The built dashboard fetches **`./config.json` RELATIVE TO THE SERVED ROOT** (`src/config.ts`:
`CONFIG_URL = './config.json'`). A copy at the delivery root is **invisible to it** — the page loads
and **silently fails to authenticate**.

**The history, recorded so it is not re-broken:**

| when | what | where config.json went |
|---|---|---|
| 2026-07-21 | `c32f858` + plugin doc `6d01bc9` | `dashboard\` — **they AGREED** |
| 2026-07-22 | Tauri commit `7963be5` | moved to the delivery root — **correct for a desktop app** |
| 2026-08-20 | `m27` returns to the browser | back to `dashboard\` — **the doc is correct again** |

⇒ **The doc was stale FOR THE TAURI ERA ONLY.** Left as it was, `m27` would have shipped a dashboard
that **loads and silently fails to authenticate**.

**`Setup.bat` now ASSERTS the config is fetchable OVER HTTP** (`serve_dashboard.py --verify-only`) and
fails at write time rather than leaving it for `selfcheck` — **a served-root relative path fails in
ways a file-exists check cannot see.** Proven in both directions: config present → OK exit 0, config
removed → FAIL exit 4.

---

## 17. 🆕 THE FOLIAGE PREMISE WAS REFUTED FROM OUR OWN BANKED DATA — and the cost is bigger than stated

### 17.1 The stated reason does not survive

The reason given for excluding foliage was *"HISM keeps rendering after `SetActorHiddenInGame`, so
foliage never actually hides."* 🚨 **THAT IS NOT SUPPORTED, and our own banked measurement points
against it.** Journal 045 Parts Nine/Ten, **post marker-correction**: hiding the foliage moved the
frame by a **whole-frame mean of 0.0059**, concentrated in **4 of 64 grid cells, peak 0.1242**,
localised exactly where the bushes are. **If the hide did nothing that number is zero.** And
`Actor.cpp:4556` shows `SetActorHiddenInGame` calling `SetHidden()` +
`MarkComponentsRenderStateDirty()` with **no foliage opt-out found in source**.

⇒ **The July 2026 OBSERVATION is not called wrong; the EXPLANATION attached to it was never verified
and must not be written as the reason again.** `G120`'s shape, caught before it reached a doc.

**The real reason, which does survive: ITS LABEL IS UNUSABLE.** `coverage_pct` reads **100** and
`bbox_px` reads **the entire frame** while roughly **1.4 %** of it changes. A label that boxes the
whole frame teaches a detector that unchanged pixels are anomalous — **the same failure mode as a
labelled-but-invisible sample, and worse than no label at all.**

### 17.2 🆕 THE COST IS A THIRD OF THE VARIETY, NOT 0.6 % OF THE ACTORS

The expectation was "2 of ~350 actors". **The selectable pool is not the actor count.** In MainWorld's
settled view the **renderable-visible selectable pool is about SIX actors**, two of which were
foliage ⇒ the exclusion removes **roughly a third of the selectable variety**, not a rounding error.

⚠ **AND THE FIRST `m27` RUN CORRECTED ME BACK.** I predicted the pool would thin to four. It did
not — the seeded selection **backfilled**, and the `m27` auto-pool leg selected **six distinct
actors** (`BP_Elevator_C`, `BP_SpawnPad_C`, `BP_SplineSpawn_C`, `BP_Stomper_C`,
`RoomBuilderSquare_C`, `SM_Ramp2`), two of which — `BP_SpawnPad_C` and `SM_Ramp2` — had **not**
appeared in the pre-change smoke. **The variety count held at six; its MEMBERSHIP changed.**
⛔ **`n = 1` leg, one map, one pose. Not a general claim about pool size.**

**`G140`** records the consequence that does generalise: **the same seed now picks different targets,
and every banked MainWorld auto-pool run is NON-COMPARABLE across this commit.**

### 17.3 The mechanism is NOT cured

⛔ **The August ruling that a class blacklist is not a fix for `H5` STANDS — scoped, not reversed.**
It was about **curing `H5`**, and **oversized bounds stay open for every other actor**.
`BP_SpawnPad_C` is the named example — a **plain `StaticMeshComponent`** at `poll_distance −114.8`
that **no foliage blacklist touches**. The over-claim rule remains the cure and remains unbuilt.

---

## 18. GATE RESULTS — every gate, and the build each ran on

🚨 **TWO COOKS. The legs do not all share a binary, and that is by design:** leg 1 tests the
**absence** of the ini key, which can only be tested on a build whose cooked ini lacks it.

| build | exe | utoc | ucas | pak | cooked ini |
|---|---|---|---|---|---|
| **cook #1** | `18081D39` | `9ABDEC35` | `AED3DEC2` | `2047F41D` | **no `[AnomalyCapture]`** |
| **cook #2** | `18081D39` | `72262793` | `6C26C482` | `0BEA8D24` | `bMaskMeasureDefault=True` |

🚨 **`G121`, THIRD RECORDED INSTANCE, AND IT IS THE CLEANEST ONE YET: THE EXE HASH IS IDENTICAL
ACROSS BOTH COOKS — `18081D39`, SAME MTIME — WHILE ALL THREE CONTAINER FILES CHANGED.** No code
changed, so nothing compiled and the archived exe kept its compile time. **Two builds answer to the
same exe hash.** Build identity is **exe + pak quartet**, and here the exe half carries **zero**
information.

Both quartets are preserved: `_binary_baselines\m27-cook1-nokey-build\` (**6/6 hash-verified at the
new location**, A62).

### 18.1 GATE 1 — `bTagFailed` cannot manufacture a `MEASURED_ZERO` — **PASS, from source**

`ArmIfMeasurable` does `if (Tagged <= 0) { R.bTagFailed = true; continue; }` at
`AnomalyMaskMeasure.cpp:180-185`, and that **`continue` PRECEDES** `ArmMask` (`:188`),
`++ArmsIssued` (`:189`) and `ArmedRequestToRecord.Add` (`:190`). ⇒ a tag failure produces **no arm,
no record, and therefore no contributing frame**, so the event lands **`NOT_MEASURED`**, which
**ADMITS**. The dangerous direction is closed structurally.

### 18.2 GATE P — the bundler — **PASS, run TWICE**

Built into an empty folder: **9/9 entries verified, 24 files**, then served **from that folder only,
with no repo access** — `index.html`, `config.json` and the JS bundle all **HTTP 200** with correct
forced MIME types. **Zero minimality exceptions.** Pointed at a bogus plugin repo it **exits 2 with
no bundle left behind**. 📌 **Re-run after the manifest changed, because a gate result does not
survive a change to the thing it gated.**

### 18.3 GATE F — foliage exclusion — **PASS, both halves**

Measured on cook #1's packaged auto-pool leg (`M27_L1_AUTOPOOL`, `session_20260820-203608`):

- **ZERO** occurrences of `InstancedFoliageActor` across **`annotation.json`, `labels.jsonl` AND
  `selection_provenance.json`**.
- **NON-ZERO others:** 8 events over the six distinct actors listed in §17.2 — the pool did not go
  thin.

⚠ **`IAI.DumpVisible`'s set-identity assertion was NOT exercised** — the bench harness never issues
it, and an early grep of mine appeared to find it only because `Select-String` is case-insensitive
and matched `blinking: matched 1 actor(s)`. **Stated as not-run rather than reported as a pass.**
Structurally both entry points read the one predicate, so they cannot disagree — **but that is a
source argument, not a measurement.**

### 18.4 GATE 2 — the browser path, end to end — **PASS**

Bundle assembled to a clean folder → owner-supplied `config.json` placed in `dashboard\` →
`write_config.py` set `capturesRoot` and reported **`controlToken = (present, unchanged)`** →
`serve_dashboard.py --verify-only` **OK exit 0** → served on `5180` → packaged game launched with
`IAI.Server.Start`.

**The enforced token was read from the RUNNING PROCESS's own log** (`G118`/`G119`), not the source
ini: **64-char, matching, no placeholder.** ⇒ **`G118` stays closed on the new cook.**

In the browser: **`GET http://127.0.0.1:5180/config.json → 200 OK`**, zero console errors, and the
dashboard rendered **`LIVE` … `connected to server`** with real live state — FPS 121, seed
751420014, poll 18 m, coverage 6 %, `OBJECTS ON SCREEN (1)`, and a live preview at `1280×720 #12`.
**The config.json relocation of §16 is proven by the one request that would have 404'd without it.**

### 18.5 GATE 3 — the ini key, all four legs — **PASS**

All four on **cook #2** except leg 1, which is the no-key case and therefore ran on **cook #1**.
Every reading is taken **from the StartRun echo (A48)**, never from the value set.

| leg | condition | echo says | vetoes | events in `annotation.json` |
|---|---|---|---|---|
| **1** | key **absent** | *mask off, default from **COMPILED DEFAULT (off); no ini key present*** | 0 | 8 |
| **2** | key **True** | *mask **ON** …, default from **DefaultGame.ini `[AnomalyCapture] bMaskMeasureDefault`*** | **6** | **2** |
| **3** | key True **+ `IAI.Capture.Mask 0`** | *mask **off*** | 0 | 8 |
| **4** | key True **+ delivery ON** | *mask **ON** …, default from **DefaultGame.ini*** | **6** | **2** |

**Leg 1** is the deliverable's whole point: **before `m27` this line did not exist at all when the
mask was off.**

**Leg 3 proves the bisect beats the ini** — the `Initialize` banner still reports *mask ON from
`DefaultGame.ini`* (correct: that is the ini-derived default at Initialize time) while the StartRun
echo reports **mask off** for the run.

**Leg 4** proves the readout survives delivery mode: **all six `VETOED-OBJECT` lines present**,
`translucent_vetoes` and `translucency_unknown_vetoes` present in `run_summary.json`, and the
delivered file set is exactly **`Actual_Frames` + `annotation.json` + `run_summary.json`** — no
`labels.jsonl`, no `run.json`, no `selection_provenance.json`. **The veto outcome is IDENTICAL to
leg 2's**, which re-asserts `G-9` orthogonality at `m27`.

**G88 satisfied positively:** `bMaskMeasureDefault` was read back **out of the cooked
`StackOBot-Windows.pak`** (1 hit), `TESTVALUE123` absent, and **there is no loose `Config` directory
beside the package at all**, so the silent-no-op hazard is not merely avoided — it is absent.

### 18.6 🚨 THE COUNT GATE — `vetoed_events` EQUALS what actually went — **VERIFIED, NOT ASSUMED**

This replaced what the two-key split was buying. `vetoed_events` is **the owner's ONLY signal that
anything was deleted** — a vetoed event leaves **no trace** in `annotation.json` — so an off-by-one
in it is **completely silent to him**.

Legs 1 and 2 ran the **same seed, same map, same binary-exe, same selection**, so leg 1's artifact is
a true BEFORE picture. Differencing the two event sets by `(target, anomaly_type, start_frame)`:

- events present in leg 1 and **missing** from leg 2 = **6**
- `run_summary.vetoed_events` = **6**
- log `countedEventsBefore=8 countedEventsAfter=2 vetoedEvents=6 nonManifestedEvents=0`

⇒ **6 = 6 = 8 − 2, and the six missing events are exactly the six named in the `VETOED-OBJECT`
lines.** The counter is correct against the artifact, not against itself.

---

## 19. 🆕 FINDINGS FROM THE GATE RUN — the new readout earned its keep immediately

### 19.1 THE ACCEPTED COST NOW HAS ITS FIRST MEASUREMENT: `translucent_vetoes = 1`

The owner accepted route (e)'s cost **without anyone having measured it**. `A2` existed to make it
measurable. **On the first leg that could report it, it reported one:**

```
VETOED-OBJECT target=BP_SplineSpawn_C_… asset=SM_GenericPlane
  componentClass=InstancedStaticMeshComponent state=MEASURED_ZERO maxCount=0
  translucency=TRANSLUCENT translucentSlots=1/1 anomaly=blinking
```

**1 of 6 vetoes on this leg is a translucent target** — every material slot it has is translucent.
⛔ **This is NOT a claim that the veto was wrong.** It is the readout doing its job: a delivered
session can now be asked the question at all.

### 19.2 🚨 AND THE SAME LEG CONSTRAINS ROUTE (e) — a within-session counter-example

**The same actor, the same session, the same binary, thirty-two frames apart:**

| event | tag | state | `maxCount` | `pctOfFrame` | buckets | `skippedHidden` |
|---|---|---|---|---|---|---|
| `blinking` `startFrame=52` | 203 | **`MEASURED_ZERO`** | 0 | 0.0000 | **all zero**, `framesContributed=4` | 5 |
| `missing_texture` `startFrame=84` | 205 | **`MEASURED_NONZERO`** | **114,724** | **12.4484** | **all zero**, `framesContributed=4` | 0 |

Both events armed 4, resolved 4, contributed 4, with `framesDiscarded`, `framesResidual`,
`framesUnconfirmed`, `framesNoPass`, `probeArms`, `collisions` and `tagFailed` **all zero**. **Both
zeros are fully interpretable; neither is an unexamined one.**

🚨 **If a translucent material simply could not write custom depth, this actor would read zero in
BOTH events. It read 12.45 % of the frame in the second.** ⇒ **for this target, translucency is NOT
sufficient to prevent the mask from seeing it.**

⛔ **NO MECHANISM IS CLAIMED AND NONE IS GUESSED (`G120`).** The observation is recorded; the
explanation is not. The difference in `skippedHidden` (5 vs 0) is **visible in the data and is a
LEAD, not a finding** — `blinking` hides its target and `missing_texture` does not.

📌 **CONSEQUENCE THAT DOES TRAVEL, and it is about how to READ the new counter:
`translucent_vetoes` counts a property of the TARGET. It is not evidence that translucency caused
the zero, and this very leg contains a counter-example.** The shipped log wording already hedges
correctly — *"its zero **may** mean the mask could not see it"* — and that hedge is now
load-bearing rather than decorative.

⛔ **NOT INVESTIGATED. `H6` remains DOCUMENTED, NOT FIXED.**

### 19.3 ⚠ AN HONEST LIMITATION IN THE PROVENANCE STRING

When the mask is **ON**, the echo names its source exactly: *"default from `DefaultGame.ini`
`[AnomalyCapture] bMaskMeasureDefault`"*.

When the mask is **OFF**, the StartRun line reads *"default from **COMPILED DEFAULT (off) or
`IAI.Capture.Mask`**"* — **a disjunction.** It cannot, on its own, distinguish *"the key is
missing"* from *"a console command turned it off"*. Those are exactly the two states `G139` was
minted to separate.

**It IS resolvable — but only by reading TWO lines.** The `Initialize` banner reports the
ini-derived default with exact provenance (leg 1: *"COMPILED DEFAULT (off); **no ini key
present**"*; leg 3: *"mask ON …, from `DefaultGame.ini`"*), and the StartRun line reports the
effective value for the run. **The pair resolves it; the StartRun line alone does not.**

⛔ **Reported as a limitation, NOT fixed** — the gate as written passes on its stated terms, and a
same-turn change to a line four legs were just read from is exactly the move this project does not
make.

---

## 20. WHAT WAS DELIBERATELY LEFT OUT

- ⛔ **NO disclosure about vetoed labels, `H6`, or the known limitations was added to
  `client-readme.md`.** That is a **COMMS decision**, and it is the owner's.
- ⛔ **`L3` remains unfixed and is now visible in `m27`'s own artifacts:** leg 2's `labels.jsonl`
  asserts positives for events that `annotation.json` no longer contains. **No client impact** —
  delivery mode never writes `labels.jsonl` — but owner-side overlay tooling will draw boxes for
  vetoed events.
- ⛔ **`G118`'s cooked-placeholder finding**: closed by this cook as a matter of fact (the enforced
  token is the real one), but the underlying *"the guard checks the source ini"* gap is unchanged.
- ⛔ **`SM_GratIng`'s drawn-to-claimed ratio was not measured in any leg**, per standing instruction.

---

## 21. STATE AFTER THIS SESSION

**Plugin:** 3 commits unpushed (`0d5e458`, `409b67a`, `9f86600`), tree clean, **`m26` tag NOT
moved**, **`feature/stencil-capture` untouched at `76cac74`**.
**AnomDash:** 2 commits unpushed (`2a62c2b`, `b8273c2`), tree clean.

**Banked this arc:** `M27_M27_L1_AUTOPOOL`, `M27_M27_L2_INIKEY_ON`, `M27_M27_L3_BISECT_OFF`,
`M27_M27_L4_DELIVERY_ON`. Bank re-checked before the cook: **zero unbanked** (`G92`). Disk 39.85 GB
at cook time (**GO**).

**Gotchas minted across the whole of session 046:** `G136`–`G138` (§7, the `I11` arc) and
**`G139`** (echo effective value AND provenance), **`G140`** (seeded-selection comparability
boundary), **`G141`** (PowerShell `-Encoding utf8` writes a BOM — it corrupted files **twice** in
this session; the safe alternative is the editor tool, or `File.AppendAllText` with
`UTF8Encoding($false)`).

⚠ **SUPERSEDED BY §22-§26 BELOW: THE SMOKE RAN AND PASSED, AND `m27` IS TAGGED AND
PUSHED.** Left as written because the sequence is part of the record.

⛔ **NOT DONE, and it is the last gate before the tag: THE OWNER'S PLAY-GATE SMOKE.** `m27` is
**not tagged** and nothing is pushed until it passes.

---

## 22. THE OWNER PLAY-GATE SMOKE — PASSED, AND IT PRODUCED THE ARC'S MOST DECISION-RELEVANT FINDING

Run by the owner in **real gameplay on `MainWorld`**, packaged BenchGate build
(`exe 18081D39 + utoc 72262793`), auto-pool, delivery off, 90 frames, 7 bursts.

⚠ **HE CAPTURED TWICE, and that turned out to matter twice over** — once because it
exercised both sides of the veto, and once because it broke my checker (§23).

| run | events | kept | vetoed | `mask_nopass_discards` | NOT_MEASURED |
|---|---|---|---|---|---|
| `session_20260820-211024` | 8 | 5 | **3** | 4 | 1 of 8 |
| `session_20260820-211345` | 8 | 8 | **0** | 20 | **5 of 8** |

**CHECK 1 — THE ECHO: PASS.** Exactly one *"EFFECTIVE FOR THIS RUN — mask ON (measure,
report and veto), default from `DefaultGame.ini` `[AnomalyCapture] bMaskMeasureDefault`"*
in the run's window, with the `Initialize` banner agreeing. **The cooked ini took.**

**CHECK 2 — THE DELETIONS: PASS.** `vetoed_events = 0` against **0** `VETOED-OBJECT` lines.
🚨 **AND IT WAS READ AS INTERNALLY CONSISTENT RATHER THAN AS A SILENT FAILURE, WHICH IS THE
POINT:** that run produced **ZERO `MEASURED_ZERO` events** — the breakdown is **5
`NOT_MEASURED` / 3 `MEASURED_NONZERO` / 0 `MEASURED_ZERO`**, with `mask.provided` matching
the tri-state on all eight rows — so **zero vetoes is the CORRECT OUTPUT**, not an absent
one. **A zero that has been shown to be the right zero is worth more than a green tick.**
The other run, eleven minutes earlier, deleted 3 — so both sides of the veto were exercised
in real content.

**CHECK 3 — FOLIAGE GONE, POOL ALIVE: PASS.** `InstancedFoliageActor` = **0** in
`annotation.json`, `labels.jsonl` **and** `selection_provenance.json`; 8 events kept, named,
including **`BP_MovingPlatform_C` and `BP_PressurePlate_C` — targets no bench leg has ever
selected.**

### 22.1 🚨 THE FINDING — STATED ONCE IN THE LEDGER, NOT RESTATED HERE

📌 **`docs/invisible-anomaly-mechanisms.md` → "THE CURE'S REACH IS VIEW-DEPENDENT AND CAN BE
LOW"**, beside the `H6` entry, by owner ruling. In one line: two runs eleven minutes apart on
the **same build** went **1-of-8 and 5-of-8 `NOT_MEASURED`**, so on `211345` the `m26`/`m27`
cure was effectively **INERT for five of eight events** — admitted because nothing was
measured, not because they were measured to draw.

⛔ **The designed SAFE direction, not a defect.** ⛔ **No mechanism claimed (`G120`)** — one
of the five is established (`SM_Ramp2`, the known-Nanite control, `G134`); the other four
were not chased.

⚠ **THE TRANSFERABLE HALF, and it is why this went in the ledger rather than a log note: THE
BENCH LEGS STRUCTURALLY COULD NOT HAVE SHOWN IT.** They run unattended with a settled camera
and read `notMeasured=0`. **`G135`'s shape again — an instrument environment that cannot
exhibit the case, with the blindness presenting as a CLEAN PASS.** ⇒ **an owner-played run
is a DIFFERENT INSTRUMENT from a bench leg.**

---

## 23. 🆕 TWO DEFECTS IN MY OWN VERIFICATION SCRIPT — `G142`

🚨 **BOTH WERE IN THE CHECKER, NOT THE BUILD, AND EITHER WOULD HAVE MANUFACTURED A FALSE
`COUNTS DISAGREE — STOP` ON A GATE THAT WAS WORKING.** Found while reporting a pass.

1. **It assumed ONE capture run per log.** The owner captured twice, so a whole-log
   `VETOED-OBJECT` grep counted **run A's three lines** against **run B's counter of zero**.
2. **Anchoring the window on `Capture run STARTED` is wrong at BOTH ENDS, in OPPOSITE
   DIRECTIONS**, so the errors do not cancel: the per-run **echo prints BEFORE** that banner
   (first symptom: "the echo is missing" — which reads as *the ini did not take*), and the
   per-event **`M26S1 EVENT` lines print AFTER the `FINISHED` banner** (second symptom: run
   A's events printed under run B's heading, which briefly looked like 3 `MEASURED_ZERO`
   events with `vetoed_events = 0` — an apparent contradiction in the product).

**The corrected rule is in `G142`** with the measured line numbers as proof. ⚠ **The general
point is `G96` applied to the CHECKER rather than the product: a checker written in the same
session as the thing it checks has never been exercised against a known-good input.** And a
**FALSE failure is expensive here** — it costs a round trip and teaches the owner to distrust
a gate that was correct.

---

## 24. BANKING — THE FIFTH UNBANKED-EVIDENCE INCIDENT

Both smoke sessions were sitting **unbanked** in the package tree and are now at
`_bench_sessions_bank\M27_OWNER_PLAYGATE_SMOKE\`, **manifest-verified 95/95 files each**.

🚨 **`StackOBot.log` WAS BANKED WITH THEM, AND THAT IS THE NEW LESSON: A VETOED EVENT LEAVES
NO TRACE IN `annotation.json` BY DESIGN**, so the `VETOED-OBJECT` lines naming *which three
objects `211024` deleted* existed **nowhere else on disk**. Banking the sessions and not the
log would have preserved the artifacts and **lost the finding**.

📌 **`G92` now carries the RUNNING COUNT — five incidents, two of them on 2026-08-20 — because
the pattern is the finding and not any single instance.** What the five share: **every one was
found by SESSION ID and none by directory name**, and the most valuable item is repeatedly an
**owner-played run**, because the harness banks its own legs and thereby creates exactly that
blind spot.

---

## 25. ⚖ THE THREE CLOSING RULINGS

**RULING 1 — FOUR COMMITS, DO NOT SQUASH.** ⚠ **RECORDED WITH ITS REASON SO A FUTURE SESSION
DOES NOT READ IT AS DRIFT:** the brief's *"plugin `m27` = ONE commit"* **predates** the
foliage exclusion and the `client-readme` rewrite entering scope. **Squashing would hide
which change carries which gate, and the one-commit convention exists FOR traceability** — so
honouring its letter would have defeated its purpose. Final shape, five plugin commits:
`0d5e458` (capture) · `409b67a` (viewport/foliage) · `9f86600` (client docs) · `3b91fe4`
(status + journal) · `4a92962` (play-gate findings).

**RULING 2 — THE STALE HELP STRING IS FILED, NOT FIXED.** 📌 **`G139`'s addendum**, naming the
exact string and recording that **Deliverable A3 is PARTIAL**. The trade: a re-cook **moves
the exe and invalidates all four Gate 3 legs** for a string **no gate ever read** — the same
trade already made deliberately for `G118` and for the `m26` bench binary. **Clearing rule:
fold into the next milestone that already requires a cook, with `G118`'s placeholder token.**
⚠ Its text contradicts **its own body**, so a reader meets an *internal inconsistency* rather
than a plain error.

**RULING 3 — THE REACH SWING GOES IN THE LEDGER AS A FINDING**, not a log note. Done; see
§22.1.

---

## 26. `m27` IS TAGGED AND PUSHED — THE MILESTONE IS CLOSED

| repo | pushed | tag |
|---|---|---|
| `AnomalyInjector` | `fda5874..4a92962` (5 commits) | **`m27` → `4a92962`**, annotated, carrying the scope statement |
| `AnomDash` | `3a8075d..b8273c2` (2 commits) | none, per dashboard precedent |

**Verified against the remote after pushing:** `origin/master` = `4a92962`, zero unpushed,
`m27` present on the remote, **`m26` UNCHANGED**, **`feature/stencil-capture` UNTOUCHED at
`76cac74`**, no force-push.

**The tag's scope statement carries seven limits**, the load-bearing ones being: the cure's
reach is view-dependent and can be low · `m26`'s zero-only partial-cure cost is unchanged ·
`translucent_vetoes` counts a property of the target and **not a cause** · delivered datasets
contain **no foliage anomalies at all** · A3 is partial · **`L3` is live and deliberately
unfixed** · a vetoed event leaves **no trace** in `annotation.json`.

⛔ **`H6` REMAINS DOCUMENTED, NOT FIXED. `P6` DID NOT MOVE. NO RATIO OR THRESHOLD EXISTS
ANYWHERE IN THE CODE OR THE DOCS.** ⛔ **Do not start anything new unprompted — the next
milestone is the owner's call.**
