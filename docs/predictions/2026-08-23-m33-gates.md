# m33 gate pre-registration — GameSpan re-key + encode_watcher cross-check

Committed BEFORE any leg runs. Restate verbatim before reading any result. The change under test:
`speed_ratio`'s denominator becomes the plugin-owned nominal span — `(CaptureGameTicks at last arm −
CaptureGameTicks at first arm) × (1/VideoFps)`, stamped inside `StampArmWallClock` — replacing the
`World->GetTimeSeconds()` span; the old world-clock ratio is still computed and emitted as
`run_summary.game_clock_speed_ratio` (+1 field, the only declared artifact delta; `annotation.json`
field set unchanged — P6 does not move). Chat's literal `(ArmedFrames−1)×(1/VideoFps)` form was
REJECTED (chat-withdrawn): settle/gap ticks sit inside the first→last-arm span, so it would misread
a healthy run at ratio ≈1.2–1.3 and falsely fire the honest stamp.

## Leg conditions (home, stock StackOBot, packaged BenchGate — G76/G103 hot-swap, no cook)

All legs: CB_GateLevel, 1280×720 windowed, `IAI.Capture.Config 2 4 8 4 0`, targeted
`blinking StaticMeshActor_49`, seed 777, 90 frames, delivery OFF, focus forced, A63/A62/B1
discipline via the standing harness. Control pair `M33_CTRL_A`/`M33_CTRL_B` runs on the PRE-m33
staged exe (`DCF9C192`) BEFORE the hot-swap; `M33_GA`/`M33_GB` run on the m33 exe.

## G-A — stock normal leg (`M33_GA`)

PASS requires ALL of:
1. `run_summary` carries `game_clock_speed_ratio`.
2. `|speed_ratio − game_clock_speed_ratio| ≤ 0.02` — the stock-host structural identity, verified
   empirically (each stock tick advances game time by exactly 1/VideoFps, so the two denominators
   coincide).
3. `stamped_fps` = 30 exactly and `annotation.video.fps` = 30 (healthy run; ratio within 1±0.02).
4. The finalize line's `armTicks` > 89 (the burst gaps exist inside the arm span). Stated so the
   rejected formula's error is visible in the artifact: `armTicks/89` is what the literal
   `(ArmedFrames−1)` form would have reported as the ratio on this same healthy leg.
5. Subset gate (`subset_gate.py M33_CTRL_A M33_CTRL_B M33_CTRL_A M33_GA`): expected EXIT 1 with the
   extras being EXACTLY `run_summary/game_clock_speed_ratio` (the m25 S4-3 / G158 shape: a declared
   +1 field makes the gate exit nonzero by construction). Any OTHER extra, or any invariant-core
   difference, is a FAIL. Exit 2 = A63 invalid ⇒ re-run the leg, read nothing.

FAILURE BRANCHES: (2) fails ⇒ the tick span and the world-clock span disagree on a stock host —
the structural-identity claim is wrong; STOP AND REPORT, no same-turn fix. (3) fails with ratio
> 1.02 on an unloaded box ⇒ the new denominator under-counts (the settle-gap error in some new
form); STOP.

## G-B — stock starved leg (`M33_GB`)

Same leg + `CaptureBench.Stall.GameMs 40` in ExecCmds (the I10 game-thread lever; banked model
`frame_time ≈ stall + 1.3 ms`, knee ~32 ms ⇒ predicted ratio band **[1.10, 1.45]**, centre ~1.24).

PASS requires ALL of:
1. `PROBE` counters confirm the lever fired (`gameStallFired > 0`).
2. `speed_ratio` inside [1.10, 1.45].
3. `|speed_ratio − game_clock_speed_ratio| ≤ 0.02 × speed_ratio` — both formulas see home
   starvation and agree (home game clock is honestly pinned by fixed step).
4. The honest stamp FIRES (content clock wall): `stamped_fps = round(30/speed_ratio, 3)`,
   `annotation.video.fps == stamped_fps` (fractional), and the run log carries the
   "could not hold 30 fps wall-clock" warning.

FAILURE BRANCHES: ratio ≈ 1.00 with the lever confirmed fired ⇒ the new denominator is blind at
home — worse than the defect; STOP. Ratios disagree beyond tolerance ⇒ the identity claim is wrong
in the starved regime; STOP.

## G-W — watcher fixtures (host-side, real ffmpeg, `--once`, scratch root; no engine)

Fixtures are COPIES of one banked delivery-OFF session that carries `labels.jsonl` +
`annotation.json` + frames. Marker files removed from copies so the watcher processes them.
(a) UNMODIFIED copy ⇒ NO "TIMEBASE MISMATCH" line; encodes at annotation fps; `.mp4_done` fps ==
    annotation fps.
(b) BLIND-CASE SYNTHETIC (t_wall values stretched ×2 about the first stamp — G135 named: no home
    run can produce the blind case, so it is manufactured) ⇒ ONE loud line naming both numbers;
    encodes at ≈ annotation_fps/2 (within 2 %); `.mp4_done` fps == the measured value.
(c) `labels.jsonl` DELETED ⇒ note line "cross-check unavailable (no labels.jsonl)"; annotation fps
    used; encode succeeds.
(d) `labels.jsonl` TRUNCATED to one row ⇒ note line (only 1 row matches); annotation fps used.
G96 both ways: (b) proves the loud path fires; (a) proves it stays silent on a healthy session.

## G-C — Concorde, owner-run, post-cook (rides the delivery checklist; NOT run at home)

Starved capture at 3200×2000 ⇒ honest stamp fires (`annotation.video.fps` ≈ measured wall rate);
MP4 plays correct speed BY STOPWATCH; `game_clock_speed_ratio` reads ≈ 1.000 (the blindness
signature, now visible beside the honest ratio); the watcher stays silent (agreement) or names the
disagreement correctly. Gate on the MERGE-equivalent step per the ruling; m33's home commit does
not wait on it.

## Order and process

Predictions committed → control pair on the pre-m33 exe → build, archive `DCF9C192`, hot-swap, A44
both-encodings scan (`game_clock_speed_ratio`, `legacyRatio`, `armTicks`) → G-A → G-B → subset gate
→ G-W → strip comments → diffstat → ONE plugin `feat(capture)` commit + ONE AnomDash watcher commit
→ push both. No tag; m31 stays open. m34 stays parked (A3).
